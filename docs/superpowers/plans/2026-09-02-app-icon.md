# ZzClawTerm 应用图标三平台适配实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 将 `ZzClawTerm.png` 源图转为 Windows .ico / macOS .icns / Linux hicolor PNG 三平台图标并完成集成。

**架构：** `scripts/generate-icons.py`（PIL）从入库源图一键生成全部派生物并回读自校验；生成物入库使构建机零依赖；CMake 按平台挂 .rc / Info.plist / qrc；`main.cpp` 设运行时窗口图标；`package-linux.sh` 弃用 base64 占位。

**技术栈：** Python 3 + Pillow 12、CMake 3.25、Qt 6.8+（qt_add_resources）。

**规格：** `docs/superpowers/specs/2026-09-02-app-icon-design.md`

---

### 任务 1：图标生成脚本与生成物

**文件：**
- 移动：`ZzClawTerm.png` → `resources/icons/source/ZzClawTerm.png`（根目录源图入库归位，用 `git mv` 语义——当前未跟踪则直接 `mv`）
- 创建：`scripts/generate-icons.py`
- 创建：`resources/icons/generated/zzclawterm.ico`、`zzclawterm.icns`、`png/{16,24,32,48,64,128,256,512}.png`（脚本产出）

- [ ] **步骤 1：移动源图并建目录**

```bash
mkdir -p resources/icons/source resources/icons/generated/png
mv ZzClawTerm.png resources/icons/source/ZzClawTerm.png
```

- [ ] **步骤 2：编写生成脚本**

`scripts/generate-icons.py` 完整内容：

```python
#!/usr/bin/env python3
"""从 resources/icons/source/ZzClawTerm.png 一键生成三平台图标产物。

用法：python3 scripts/generate-icons.py
生成后自动回读校验（ico 尺寸齐全、icns 条目表完整、png 可解析）。
"""
import io
import struct
import sys
from pathlib import Path

from PIL import Image

ROOT = Path(__file__).resolve().parent.parent
SOURCE = ROOT / "resources" / "icons" / "source" / "ZzClawTerm.png"
OUT = ROOT / "resources" / "icons" / "generated"

ICO_SIZES = [16, 24, 32, 48, 64, 128, 256]
PNG_SIZES = [16, 24, 32, 48, 64, 128, 256, 512]
# (类型码, 边长)；icp4/5/6 为 1x 小尺寸组，ic07-ic10 为 128-1024，ic11-ic14 为 @2x 槽位
ICNS_ENTRIES = [
    ("icp4", 16), ("icp5", 32), ("icp6", 64), ("ic07", 128),
    ("ic08", 256), ("ic09", 512), ("ic10", 1024),
    ("ic11", 64), ("ic12", 32), ("ic13", 256), ("ic14", 512),
]
NEED_SIZES = sorted({s for _, s in ICNS_ENTRIES} | set(PNG_SIZES) | set(ICO_SIZES))


def render() -> dict[int, Image.Image]:
    img = Image.open(SOURCE).convert("RGBA")
    return {s: img.resize((s, s), Image.LANCZOS) for s in NEED_SIZES}


def write_pngs(images: dict[int, Image.Image]) -> None:
    png_dir = OUT / "png"
    png_dir.mkdir(parents=True, exist_ok=True)
    for s in PNG_SIZES:
        images[s].save(png_dir / f"{s}.png", optimize=True)


def write_ico(images: dict[int, Image.Image]) -> None:
    ordered = [images[s] for s in ICO_SIZES]
    ordered[0].save(
        OUT / "zzclawterm.ico", format="ICO",
        append_images=ordered[1:], sizes=[(s, s) for s in ICO_SIZES],
    )


def write_icns(images: dict[int, Image.Image]) -> None:
    body = b""
    for code, s in ICNS_ENTRIES:
        buf = io.BytesIO()
        images[s].save(buf, format="PNG", optimize=True)
        data = buf.getvalue()
        body += code.encode("ascii") + struct.pack(">I", 8 + len(data)) + data
    (OUT / "zzclawterm.icns").write_bytes(
        b"icns" + struct.pack(">I", 8 + len(body)) + body)


def verify() -> None:
    with Image.open(OUT / "zzclawterm.ico") as ico:
        ico.load()
        frames = getattr(ico, "n_frames", 1)
        assert frames == len(ICO_SIZES), f"ico 帧数 {frames} != {len(ICO_SIZES)}"

    raw = (OUT / "zzclawterm.icns").read_bytes()
    assert raw[:4] == b"icns", "icns magic 错误"
    total = struct.unpack(">I", raw[4:8])[0]
    assert total == len(raw), f"icns 长度字段 {total} != 实际 {len(raw)}"
    codes = set()
    off = 8
    while off < total:
        code = raw[off:off + 4].decode("ascii")
        size = struct.unpack(">I", raw[off + 4:off + 8])[0]
        assert size >= 8 and off + size <= total, f"icns 条目 {code} 长度非法"
        Image.open(io.BytesIO(raw[off + 8:off + size])).verify()
        codes.add(code)
        off += size
    want = {c for c, _ in ICNS_ENTRIES}
    assert codes == want, f"icns 条目缺失: {want - codes}"

    for s in PNG_SIZES:
        with Image.open(OUT / "png" / f"{s}.png") as im:
            im.verify()
    print("校验通过：ico 7 尺寸 / icns 11 条目 / png 8 尺寸")


def main() -> int:
    if not SOURCE.exists():
        print(f"源图不存在: {SOURCE}", file=sys.stderr)
        return 1
    images = render()
    write_pngs(images)
    write_ico(images)
    write_icns(images)
    verify()
    print(f"产物目录: {OUT}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **步骤 3：运行生成并校验**

运行：`python3 scripts/generate-icons.py`
预期：输出「校验通过：ico 7 尺寸 / icns 11 条目 / png 8 尺寸」与产物目录路径，退出码 0。

- [ ] **步骤 4：幂等复查（再跑一次，产物字节一致）**

运行：`python3 scripts/generate-icons.py && md5sum resources/icons/generated/zzclawterm.ico resources/icons/generated/zzclawterm.ico`（两次运行间产物哈希不变——因 PNG optimize 确定性；此处复查第二次运行退出码 0 即可，哈希对比人工抽查）

### 任务 2：qrc 资源与 CMake 三平台接线

**文件：**
- 创建：`resources/icons/appicon.qrc`
- 创建：`src/windows/zzclawterm.rc`
- 创建：`src/macos/Info.plist`
- 修改：`src/CMakeLists.txt`（`qt_add_executable(ZzClawTerm ...)` 段，约 :93-101）

- [ ] **步骤 1：创建 qrc**

`resources/icons/appicon.qrc`：

```xml
<RCC>
    <qresource prefix="/icons">
        <file alias="zzclawterm.png">generated/png/256.png</file>
    </qresource>
</RCC>
```

- [ ] **步骤 2：创建 Windows 资源脚本**

`src/windows/zzclawterm.rc`：

```rc
IDI_ICON1 ICON "..\\..\\resources\\icons\\generated\\zzclawterm.ico"
```

- [ ] **步骤 3：创建 macOS Info.plist**

`src/macos/Info.plist`（基于 Qt6 模板骨架，`@MACOSX_BUNDLE_*@` 由 CMake 代入）：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleExecutable</key>
    <string>@MACOSX_BUNDLE_EXECUTABLE_NAME@</string>
    <key>CFBundleName</key>
    <string>@MACOSX_BUNDLE_BUNDLE_NAME@</string>
    <key>CFBundleIdentifier</key>
    <string>com.zzclawterm.app</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>@MACOSX_BUNDLE_SHORT_VERSION_STRING@</string>
    <key>CFBundleVersion</key>
    <string>@MACOSX_BUNDLE_BUNDLE_VERSION@</string>
    <key>CFBundleIconFile</key>
    <string>@MACOSX_BUNDLE_ICON_FILE@</string>
    <key>LSMinimumSystemVersion</key>
    <string>12.0</string>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
    <key>NSSupportsAutomaticGraphicsSwitching</key>
    <true/>
</dict>
</plist>
```

- [ ] **步骤 4：修改 src/CMakeLists.txt**

把 `qt_add_executable(ZzClawTerm main.cpp)` 段改为：

```cmake
qt_add_executable(ZzClawTerm main.cpp)
set_target_properties(ZzClawTerm PROPERTIES
    WIN32_EXECUTABLE TRUE
    MACOSX_BUNDLE TRUE
)
if(WIN32)
    # Windows 应用图标：.rc 把多尺寸 ico 编译进 exe 资源
    target_sources(ZzClawTerm PRIVATE windows/zzclawterm.rc)
endif()
if(APPLE)
    set(ZZ_ICNS "${CMAKE_SOURCE_DIR}/resources/icons/generated/zzclawterm.icns")
    set_source_files_properties("${ZZ_ICNS}" PROPERTIES
        MACOSX_PACKAGE_LOCATION Resources)
    target_sources(ZzClawTerm PRIVATE "${ZZ_ICNS}")
    set_target_properties(ZzClawTerm PROPERTIES
        MACOSX_BUNDLE_ICON_FILE zzclawterm.icns
        MACOSX_BUNDLE_BUNDLE_NAME "ZzClawTerm"
        MACOSX_BUNDLE_BUNDLE_VERSION "0.1"
        MACOSX_BUNDLE_SHORT_VERSION_STRING "0.1"
        MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/macos/Info.plist"
    )
endif()
# 运行时窗口图标（Wayland 任务栏必需）：qrc 嵌 256px PNG
qt_add_resources(ZzClawTerm "../../resources/icons/appicon.qrc")
```

注意保留原有 `target_link_libraries` 与 `install` 段不动。

- [ ] **步骤 5：Linux 本机配置验证**

运行：`cmake --preset linux-gcc-debug 2>&1 | tail -5 && cmake --build --preset linux-gcc-debug 2>&1 | tail -5`
预期：配置与编译通过（qrc 参与构建；win/apple 分支不触发）。

### 任务 3：运行时窗口图标

**文件：**
- 修改：`src/main.cpp`（`QApplication` 构造之后）

- [ ] **步骤 1：加 setWindowIcon**

在 `main.cpp` 中 `QApplication app(argc, argv);`（或等价行）之后插入：

```cpp
    // 运行时窗口图标：Wayland 任务栏图标唯一来源；与三平台原生图标同源
    app.setWindowIcon(QIcon(QStringLiteral(":/icons/zzclawterm.png")));
```

文件头部 `#include` 区补 `#include <QIcon>`（若无）。

- [ ] **步骤 2：全量构建 + 全量测试**

运行：`cmake --build --preset linux-gcc-debug && ctest --preset linux-gcc-debug 2>&1 | tail -15`
预期：构建 0 错误；测试 48/48 通过。跑完按铁规矩清理 `tests/perf/records/`（`git checkout -- tests/perf/records/` + 删除当天未跟踪 json）。

### 任务 4：Linux 打包脚本接入正式图标

**文件：**
- 修改：`scripts/package-linux.sh:25-30`（base64 占位段）

- [ ] **步骤 1：替换 base64 段**

把「桌面入口与图标」注释、`mkdir`（hicolor 单尺寸）与 `cat <<'B64' ... B64` 三处替换为：

```bash
# 桌面入口与正式图标（由 scripts/generate-icons.py 从源图生成）
mkdir -p "$APPDIR/usr/share/applications"
for size in 16 24 32 48 64 128 256 512; do
    mkdir -p "$APPDIR/usr/share/icons/hicolor/${size}x${size}/apps"
    cp "$ROOT/resources/icons/generated/png/${size}.png" \
       "$APPDIR/usr/share/icons/hicolor/${size}x${size}/apps/zzclawterm.png"
done
```

- [ ] **步骤 2：语法与产物检查**

运行：`bash -n scripts/package-linux.sh`
预期：无语法错误。若本机 PATH 有 `linuxdeploy-x86_64.AppImage` 且 `QT_ROOT` 可用，则跑一次完整打包并检查 `dist/linux/ZzClawTerm.AppDir/usr/share/icons/hicolor/` 各尺寸 png 存在；否则本步以语法检查为准，真机打包列入验收清单。

### 任务 5：收尾提交

**文件：**
- 提交：`resources/icons/`（source + generated + qrc）、`scripts/generate-icons.py`、`src/windows/zzclawterm.rc`、`src/macos/Info.plist`、`src/CMakeLists.txt`、`src/main.cpp`、`scripts/package-linux.sh`、两份 docs（spec 与 plan）

- [ ] **步骤 1：确认 git status 只含本计划文件**

- [ ] **步骤 2：提交（Conventional + 中文，用户已批准本次提交）**

```bash
git add resources/icons scripts/generate-icons.py src/windows src/macos src/CMakeLists.txt src/main.cpp scripts/package-linux.sh docs/superpowers/specs/2026-09-02-app-icon-design.md docs/superpowers/plans/2026-09-02-app-icon.md
git commit
```

提交信息：

```
feat(图标): 三平台应用图标适配（Windows/Linux/macOS）

由 scripts/generate-icons.py 从 1254px 源图一键生成 .ico（7 尺寸）、
.icns（11 条目含 @2x）与 hicolor PNG（8 尺寸）并回读自校验，
生成物入库使构建机零 Python 依赖。

- Windows：src/windows/zzclawterm.rc 把 ico 编译进 exe 资源
- macOS：src/macos/Info.plist + MACOSX_BUNDLE_ICON_FILE 打进 Resources
- Linux：package-linux.sh 弃用 base64 占位，复制正式多尺寸图标
- 运行时：appicon.qrc 嵌 256px PNG，main.cpp setWindowIcon，
  Wayland 任务栏图标生效
```

- [ ] **步骤 3：推送前询问用户**（推送需逐次批准；附 Windows/macOS 真机验收清单）

/**
 * @file ZzTerminalCorePrivate.cpp
 * @brief ZzTerminalCore 私有实现 (当前逻辑内联于头文件)。
 */

#include "core/terminal/ZzTerminalCorePrivate.h"

namespace ZzCore {
namespace Terminal {

// 私有实现目前完全内联于头文件; 保留本翻译单元以符合四文件结构,
// 并为后续扩展 (备用屏、制表位等) 预留位置。

}  // namespace Terminal
}  // namespace ZzCore
